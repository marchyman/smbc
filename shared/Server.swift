/// The server URL as a string and the name of the folder used to hold
/// most of the schedule data
///
let serverName = "https://smbc.snafu.org/"
let serverDir = "schedule/"

// year data (schedules exist for the listed years)
let yearModelName = "schedule-years.json"

// schedule data
let scheduleBase = "schedule"
let scheduleExt = "json"
let scheduleFileName = scheduleBase + "." + scheduleExt

// restaurant data
let restaurantFileName = "restaurants.json"

// trip data
let tripFileName = "trips.json"

// gallery data
let galleryFileName = "gallery.json"
